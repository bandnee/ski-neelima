Connection-objectPool{
    int max

    #mutable Data
    List[0] freeList
    int allocated

    #
    mutex m
    cv objAvailable
}



free(obj) {
    m.wait()
    freeList.add
    objAvailable.signal
    m.signal()
}

Obj get() {
    m.wait()
    while(freeList.size==0 and allocated == max):
        objAvaialble.wait()
    Obj O
    if(freeList.size() < 0){
        O = freeList.pop()
    } else {
        ++ allocated
    }
    m.signal()
    return 0





}